{
    "name": "selling-app",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "format": "prettier --parser typescript --single-quote --write **/*.ts"
    },
    "husky": {
        "pre-commit": "npm run format"
    },
    "devDependencies": {
        "@types/node": "^14.0.14",
        "husky": "^4.2.5",
        "prettier": "^1.19.1"
    }
}
